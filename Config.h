#pragma once

#include "raylib.h"

// Pantalla
const int screenWidth = 800;
const int screenHeight = 600;

// Tren y Movimiento
const float TREN_VELOCIDAD = 200.0f;

// Juego
const float TIEMPO_LIMITE_PREGUNTA = 5.0f;
const int CARRIL_ALTURA = 150; // Distancia entre carriles al bajar

// Colores (varios de aca no los necesito como los de lo vagones, pero para probrar)
const Color COLOR_FONDO = SKYBLUE;
const Color COLOR_TREN = RAYWHITE;
const Color COLOR_VAGON = RAYWHITE;
const Color COLOR_TEXTO = RAYWHITE;
